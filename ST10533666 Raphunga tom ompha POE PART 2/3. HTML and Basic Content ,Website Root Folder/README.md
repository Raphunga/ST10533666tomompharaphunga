# Mpho's Bakery – Part 2 CSS Styling and Responsive Design

## Part 2 work completed
- Created one external stylesheet: `css/style.css`.
- Linked the stylesheet to all supplied HTML pages.
- Added a CSS reset and consistent base typography.
- Added a consistent colour scheme, spacing, borders and shadows.
- Added Flexbox navigation.
- Added Grid-based page layout.
- Added `:hover`, `:focus` and `:active` states.
- Added tablet and mobile media-query breakpoints.
- Added relative units such as `rem`, `%` and `vw`/`min()`.
- Added responsive image `srcset` and `sizes` attributes using the existing image assets.
- Improved form styling and mobile form layout.
- Added accessibility-friendly focus outlines.
- Cleaned unnecessary `<br><br>` spacing from the forms.

## Responsive breakpoints
- Mobile: up to 767px
- Tablet: 768px and above
- Desktop: 1024px and above
- Small phones: up to 480px

## Part 1 Feedback
No feedback was provided.

## Part 2 changelog
- Added external CSS stylesheet and linked it to all supplied pages.
- Added reset/base styles and typography.
- Added responsive navigation and layout.
- Added desktop/tablet/mobile media queries.
- Added responsive image attributes.
- Added form controls and interactive states.
- Updated README with Part 2 implementation details.

## Testing evidence
Take screenshots in browser developer tools for:
1. Desktop view
2. Tablet view
3. Mobile view
4. A second mobile/tablet device size if required by the lecturer

Save the screenshots in the project and list them here with their device/viewport sizes.

## Important file note
The supplied home page was named `index(9).html`. It has been renamed to `index.html` in this Part 2 version so that the navigation links work correctly.

The `about.html` page was not supplied with the files received for this update. The navigation still points to `about.html`, so the existing Part 1 `about.html` must be placed in the project folder.

## References
Add the academic/web references required by your lecturer here, using the required Harvard referencing format.
